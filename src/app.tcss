Screen {
    layout: vertical;
}

#chat-scroll {
    height: 1fr;
    scrollbar-size: 1 1;
}

.message {
    width: 100%;
    height: auto;
    padding: 0 1;
}

#status-bar {
    height: 1;
    background: $surface;
    color: $text-muted;
    padding: 0 1;
}

#user-input {
    margin: 0;
    border: none;
    height: auto;
    min-height: 1;
    padding: 0 1;
    background: transparent;
}

#user-input:focus {
    border: none;
}

ModelSelector {
    align: center middle;
}

ModelSelector OptionList {
    width: 60;
    height: auto;
    max-height: 20;
    border: solid $primary;
    background: $surface;
}
